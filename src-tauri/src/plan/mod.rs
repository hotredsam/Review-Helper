//! Plan generation: read-only analysis of a clone (or a blank-project
//! description) into a parsed, persisted, versioned plan.

pub mod commands;
pub mod ingest;
pub mod parse;
pub mod prompts;
pub mod store;
