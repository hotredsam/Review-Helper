# Phase 18 — Polish sweep
Status: planned
Goal: Clear the remaining minor/polish findings — stale caches, races, dead code, missing doors, a11y, and repo hygiene — so the app passes its own audit end to end.
Depends on: Phases 15–17. Findings cited as A# live in `.planning/AUDIT-2026-06-09.md`.

## Tasks
- [ ] **T1 Pane-state correctness batch** — WhyExplain refetches when `term` changes (and PlanPane's decisions list keys on identity, not index) (A17); CardChat drops in-flight replies that resolve after the card switched (A18/A34); UnderstandHub ignores stale responses from a previous project (A20); quiz answers and flashcard grades lock while a request is in flight (A19); `chatStore.loadProject` gets an in-flight guard so concurrent calls can't create duplicate transcripts (A16). Done when: each has a regression test that fails on the old code.
- [ ] **T2 Chat UX batch** — autoscroll only when already near the bottom, so users can scroll up during streaming (A40); a failed turn renders an inline error state (with retry) instead of a permanent empty bubble (A42); the delete button is keyboard-reachable (`group-focus-within`, not `hidden`-until-hover) (A38). Done when: tests cover each; manual check of mid-stream scrolling.
- [ ] **T3 ProviderSettings error handling** — save failures no longer wipe the panel or claim "Couldn't load"; load and save errors are distinct, the controls stay rendered, and a successful save toasts via `setNotice` (A39/A43). Done when: a test mocks a failed save and asserts the panel survives with accurate copy.
- [ ] **T4 PDF upload without the JSON-array freeze** — stop shipping up-to-25 MB files as JS number arrays through IPC (A21/A30): pass base64 (or a temp-file path) with a friendly size-limit error and progress affordance. Done when: a large PDF uploads without freezing the UI and an oversize file errors politely.
- [ ] **T5 GitHub device-flow reconciliation** — the Rust device-flow commands have zero frontend call sites and error copy points at UI that doesn't exist (A22). Decide by reading what GithubConnect actually uses: wire the device flow UI if the intended flow is missing, otherwise delete the dead commands and fix the copy. (History: device flow was built in Phase 3 but the `gh auth token` import path was chosen instead — so delete-and-fix-copy is the likely answer unless gh-less users matter now.) Remove them from Phase 15's expected-dead list either way. Also: keep the classic `repo` scope but document in SECURITY.md why (private-repo access requires it) (A44). Done when: no dead commands remain and the contract suite's expected-dead list is empty.
- [ ] **T6 Project rename/delete UI** — surface the fully-built backend + store support (A14): rename and delete from the project switcher (Modal-confirmed delete, per the Phase 15 standard), and deleting a project also removes its shallow-clone cache directory (A26/A29). Done when: a typo'd project can be renamed or fully removed from the UI, including its cache dir.
- [ ] **T7 Tour fixes** — a stray scrim click no longer permanently dismisses the tour (explicit Skip instead); focus management reaches parity with Modal.tsx (trap, initial focus, restore); steps added for Learning mode, Palette, and Overview (A47). Done when: tests cover dismiss + focus behavior and the new steps render.
- [ ] **T8 Repo & component hygiene** — `git rm --cached scripts/__pycache__` and add `__pycache__/` + `*.pyc` to .gitignore (A52); hoist StudyView's inline `TabButton` so tabs stop remounting and dropping focus (A41). Done when: `git ls-files` shows no bytecode and a test confirms tab focus survives a click.
- [ ] **Tend Phase verification** — full keyboard-only pass over chat rail and learning tabs; upload a big PDF; rename + delete a project; replay the tour. Done when: all pass, suites green, and every audit finding except Phase 19's is closed in `.planning/AUDIT-2026-06-09.md`.

## Watch for (this phase)
- These are many small diffs across many files — keep commits atomic per task batch, and don't let a "while I'm here" refactor leak in (scope rule in CLAUDE.md).
- T5 is the only task with a real decision inside it; read the existing connect flow before choosing wire-vs-delete.
